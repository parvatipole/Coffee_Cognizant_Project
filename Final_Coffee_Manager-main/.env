# Frontend Environment Configuration
# STANDALONE MODE: Frontend works independently without backend

# Set to false when backend is ready for integration
VITE_STANDALONE_MODE=true

# API Base URL (will be used when standalone mode is disabled)
VITE_API_BASE_URL=http://localhost:8080/api

# Debug mode for development
VITE_DEBUG=true

# App Configuration
VITE_APP_NAME="Coffee Manager"
VITE_APP_VERSION="1.0.0"
